import { SwapWorkspace } from '@/components/trade/swap/SwapWorkspace';

export default function Home() {
  return (
    <main className='sm:mx-auto mx-4 py-10 h-full'>
      <SwapWorkspace />
    </main>
  );
}
