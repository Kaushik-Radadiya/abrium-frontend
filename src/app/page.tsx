import { SwapWorkspace } from '@/components/swap/SwapWorkspace'

export default function Home() {
  return (
    <main className='sm:mx-auto mx-4 py-10 h-full'>
      <SwapWorkspace />
    </main>
  )
}
